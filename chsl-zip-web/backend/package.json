{
  "name": "chsl-zip-web-backend",
  "version": "1.0.0",
  "description": "Backend for the CHSL Previous Year Papers ZIP web app (content discovery + hardened PDF proxy + background ZIP jobs)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "archiver": "^7.0.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
